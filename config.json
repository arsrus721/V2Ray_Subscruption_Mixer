{
    "type": "mixer",
    "sources": ["http://url/path/", "https://url1/path/"],
    "profile-title": "NAME VPN",
    "announce": "ABOUT",
    "v2raytun-announce": "#d9c1c1ABOUT",
    "subscription-userinfo-ord": 0,
    "profile-update-interval": 1,
    "announce-url": "https://example.com/path",
    "support-url": "https://example.com/path",
    "replace-ip": {
        "0": {
            "sni": ["www.google.com", "google.com"],
            "ip": "1.2.3.4"
        },
        "1": {
            "sni": ["www.speedtest.net", "speedtest.net"],
            "ip": "2.3.4.5"
        }
    },
    "server-settings": {
        "accept-prefix": "/cursed/sub",
        "bind": "0.0.0.0",
        "port": 8080
    }
}

DELETE THIS
----------------------
type — Necessarily str
sources — Necessarily list
profile-title — Necessarily str
announce — not Necessarily str
v2raytun-announce — not Necessarily str
subscription-userinfo-ord — Necessarily int
profile-update-interval — Necessarily int 
announce-url — not Necessarily  str
support-url — not Necessarily str  

replace-ip — not Necessarily  
replace-ip[n]:
    sni — sni's list
    ip — ip to change str

server-settings — Necessarily  
server-settings:
    accept-prefix — path str
    bind — addr str
    port — port int
-----------------------